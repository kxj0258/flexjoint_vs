# Inherit all shared hardware, vision, control, and safety settings from the
# main config. Only the experiment mode differs for this baseline ablation.
extends: robot_config.yaml

experiment:
  controller_mode: "baseline_pd_no_fast"
